{
  "options": [
    {
      "default": "./emails-src",
      "description": "Must contain an entry point — `index.ts`, `index.tsx`, `index.js` or `index.jsx` — exporting a `templates` array.",
      "id": "sourceDir",
      "summary": "Where your templates live, relative to the config file.",
      "type": "string"
    },
    {
      "default": "./emails-dist",
      "description": "Three files per template land here: the readable template, the render function, and its type declarations, plus an `index` re-exporting all of them and a `package.json`. Commit them or generate them in CI, whichever suits — they are the artifact your backend imports.",
      "id": "outputDir",
      "summary": "Where compiled output is written, relative to the config file.",
      "type": "string"
    },
    {
      "default": 7777,
      "description": null,
      "id": "previewPort",
      "summary": "Port for `mailgrail preview`.",
      "type": "number"
    },
    {
      "default": true,
      "description": "Turn it off for a JavaScript project; the `.js` render function is emitted either way.",
      "id": "typescript",
      "summary": "Whether to emit a `.d.ts` beside each compiled template.",
      "type": "boolean"
    },
    {
      "default": "EJS",
      "description": "Also selects the template file extension and the module the generated `.js` imports at run time. `mailgrail build` checks that engine is installed and fails with an actionable message if it is not, so a missing engine surfaces at build time rather than when an email is sent.",
      "id": "templatingEngine",
      "summary": "Which engine the compiled output targets.",
      "type": "\"EJS\" | \"Handlebars\" | \"Mustache\""
    },
    {
      "default": "esm",
      "description": "`esm` emits `import`/`export`, `cjs` emits `require`/`exports` for a CommonJS backend. The `package.json` written beside the output declares the matching `type`, and the build fails if a `package.json` already there contradicts it — the two disagreeing is what makes Node refuse to load the files at all.",
      "id": "moduleFormat",
      "summary": "Module system the compiled output is written in.",
      "type": "\"esm\" | \"cjs\""
    },
    {
      "default": false,
      "description": null,
      "id": "hideAppName",
      "summary": "Hide the project name in the preview UI.",
      "type": "boolean"
    },
    {
      "default": false,
      "description": null,
      "id": "hideAppDescription",
      "summary": "Hide the project description in the preview UI.",
      "type": "boolean"
    },
    {
      "default": false,
      "description": null,
      "id": "hideAppLogo",
      "summary": "Hide the logo in the preview UI.",
      "type": "boolean"
    },
    {
      "default": [],
      "description": "Each template is built once per locale. Leave it unset and localization is off: the build emits one unlocalized set, exactly as without it. Tags are canonicalized, so `en_us` and `en-US` name the same locale. The **Localization** guide walks through a full setup.",
      "id": "locales",
      "summary": "The locales to build, as BCP 47 tags.",
      "type": "string[]"
    },
    {
      "default": null,
      "description": "Defaults to the first of `locales`, and must be one of them. It is the language `defineMessages()` is written in, what a missing translation falls back to, and what a render function uses when asked for a locale it was not built for. Setting it without `locales` fails the build.",
      "id": "defaultLocale",
      "summary": "The locale the source messages are written in.",
      "type": "string"
    },
    {
      "default": "./emails-src/locales",
      "description": "Defaults to a `locales` folder inside `sourceDir`. It holds one `<locale>.json` per locale, which `mailgrail extract` creates and keeps in line with the source messages.",
      "id": "localesDir",
      "summary": "Where the translation catalogs live, relative to the config file.",
      "type": "string"
    },
    {
      "default": false,
      "description": "A missing translation, or a missing catalog file, normally warns, and the default-locale text stands in for it. With this on, it fails the build instead; turn it on in CI once your catalogs are complete. Broken translations -- a message that does not parse, or uses an argument the source does not have -- fail the build either way, and stale entries only ever warn.",
      "id": "strictLocales",
      "summary": "Fail the build on incomplete translations.",
      "type": "boolean"
    },
    {
      "default": "UTC",
      "description": "The default for every render function; a call can override it with `{ timeZone }`. The build fails on a zone the running Node does not know, rather than letting the first email that formats a date throw.",
      "id": "timeZone",
      "summary": "The IANA time zone dates are formatted in.",
      "type": "string"
    }
  ]
}
