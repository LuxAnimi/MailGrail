---
import Landing from "../layouts/Landing.astro";
import Hero from "../components/landing/Hero.astro";
import Sequence from "../components/landing/Sequence.astro";
import CodeShowcase from "../components/landing/CodeShowcase.astro";
import Pipeline from "../components/landing/Pipeline.astro";
import Features from "../components/landing/Features.astro";
import Engines from "../components/landing/Engines.astro";
import Closing from "../components/landing/Closing.astro";
import { href } from "../lib/href";
import meta from "../generated/meta.json";

const docsHref = href("/docs/getting-started/introduction");

// The closing section is the "Get started" call to action and shows the install
// command, so it points at installation rather than the introduction -- someone
// who has scrolled the whole page has had the introduction already. The hero
// keeps the introduction, where a first-time reader should land.
const startHref = href("/docs/getting-started/installation");
---

<Landing
  title="MailGrail — type-safe email templates"
  description={meta.description}
>
  <div class="wrap">
    <Hero docsHref={docsHref} />
  </div>

  <Sequence />
  <Pipeline />
  <CodeShowcase />
  <Features />
  <Engines />
  <Closing startHref={startHref} />
</Landing>
